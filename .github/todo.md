# TODO

1. electron-builder を導入する（`pnpm add -D electron-builder`、`package.json` に build 設定を追加）
2. GitHub Actions で Windows 向けインストーラ（.exe）をビルドし、GitHub Releases に添付する
3. Windows 実機でインストール・起動・アンインストールを確認する
4. デザインの大枠を作成する（レイアウト、カラー、コンポーネント構成など）
